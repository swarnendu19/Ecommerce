version: '3.8'

services:
  redis:
    image: redis:latest
    container_name: ecom-redis
    ports:
      - "6379:6379"
    environment:
      - REDIS_PASSWORD= 123
    command: redis-server --requirepass myredispassword
 